function scream(words) {
  words = words + '!!!!';
  return;
  console.log(words);
}

scream('Yipeee');

// This function does not log anything to the console
// because 'return' on line 3 terminates the function.